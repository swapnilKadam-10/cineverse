export const Button = ({children}) => {
  return (
    <button className="w-64  text-2xl font-semibold dark:text-white rounded-xl p-5 bg-gradient-to-r from-cyan-500 to-blue-500">{children}</button>
  )
}
